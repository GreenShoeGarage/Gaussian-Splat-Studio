# Failure Guide

Maker Splat classifies common failures.

Categories:

- `missing_tool`
- `bad_capture`
- `colmap_failed`
- `training_failed`
- `export_failed`
- `gpu_failed`
- `unknown`

Report file:

```text
FAILURE_CLASSIFICATION.md
```
