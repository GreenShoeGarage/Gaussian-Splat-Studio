# Datasets

The first validation dataset belongs in:

```text
datasets/first-splat/images/
```

Use 40–80 known-good photos.

Run:

```bash
./qa/run-real-mode-dataset.sh datasets/first-splat
```

Do not use difficult objects for the first validation.
